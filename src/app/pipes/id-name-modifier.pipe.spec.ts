import { IdNameModifierPipe } from './id-name-modifier.pipe';

describe('IdNameModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new IdNameModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
